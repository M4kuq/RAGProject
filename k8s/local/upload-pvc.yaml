apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: upload-storage
  labels:
    app.kubernetes.io/name: ragproject
    app.kubernetes.io/component: upload-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
